package pw.productors.secondlesson.model;

import android.util.Log;

public class MemoryManager implements MemoryHandler {

    String TAG = "SPACETAG";

    public MemoryManager() {
        Log.d(TAG, "MemoryManager: constructor");
    }
}
